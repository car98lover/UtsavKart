To run this project. write the following command on a terminal.

backend : npm i                (hit enter)
          npm run dev          (hit enter)

frontend : cd .\frontend\      (hit enter)
           npm i               (hit enter)
           npm start           (hit enter)
